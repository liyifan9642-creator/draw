# VDC 设计文档 — AI 语音绘图工具

## 1. 项目概述

VDC（Voice-Driven Canvas）是一款纯语音控制的 AI 绘图工具。用户不能使用鼠标或键盘，仅通过语音指令即可完成绘图创作。系统采用 **ASR → LLM → Tool Calling → Canvas Rendering** 的端到端管线架构，将用户的自然语言语音转化为画布上的图形操作。

### 1.1 设计目标

| 目标 | 说明 |
|------|------|
| 纯语音交互 | 用户全程通过语音完成绘图，不依赖鼠标/键盘 |
| 指令理解准确性 | LLM + 语音纠错映射，确保语音识别偏差不影响绘图结果 |
| 低响应延迟 | 从用户说完到画布渲染的全链路延迟控制在可接受范围内 |
| 复杂指令拆解 | 将复合意图（如"画一只猫坐在屋顶上"）拆解为多个原子操作 |
| 手绘风格渲染 | 所有图形以 rough.js 手绘美学呈现，增强视觉亲和力 |

---

## 2. 指令能力规划与实现情况

### 2.1 已实现的指令能力

#### 2.1.1 基础图形生成（✅ 完整实现）

支持 5 种基础几何图形的创建，每种图形支持丰富的样式参数：

| 图形 | 参数 | 定位方式 |
|------|------|----------|
| 矩形 (rect) | width, height, fill, stroke, strokeWidth, rotation, opacity, cornerRadius | 网格坐标 / 空间位置 / 绝对像素 |
| 圆形 (circle) | radius, fill, stroke, strokeWidth, rotation, opacity | 同上 |
| 三角形 (triangle) | width, height, fill, stroke, strokeWidth, rotation, opacity | 同上 |
| 椭圆 (ellipse) | radiusX, radiusY, fill, stroke, strokeWidth, rotation, opacity | 同上 |
| 线条 (line) | points, stroke, strokeWidth, opacity | 同上 |

**语音指令示例：**
- "画一个红色的圆形"
- "在画布左上角画一个蓝色矩形"
- "画一个绿色三角形在 x25y25 的位置"

**设计亮点：**
- 三种定位方式（网格坐标 > 空间位置 > 绝对像素）降低了 LLM 对像素坐标的依赖
- 50×50 网格坐标系统为 LLM 提供了直观的空间参考框架
- 空间位置枚举（center, top-left, top-right 等）进一步简化了常用定位需求

#### 2.1.2 参数化模板系统（✅ 完整实现，42 种）

覆盖 8 大类别的复杂参数化模板，每种模板由多个 SVG path 组合而成：

| 类别 | 模板数量 | 包含模板 |
|------|----------|----------|
| 动物 | 12 | cat, dog, bird, fish, rabbit, butterfly, horse, elephant, lion, penguin, owl, frog |
| 植物 | 3 | tree, flower, mushroom |
| 自然 | 2 | sun, mountain |
| 建筑 | 1 | house |
| 交通工具 | 7 | car, boat, airplane, train, bicycle, helicopter, rocket |
| 食物 | 6 | apple, coffee_cup, hamburger, donut, banana, strawberry |
| 人物 | 1 | person |
| 物品 | 10+ | crown, key, flag, robot, guitar, basketball, trophy, gift, balloon, umbrella, hat |

**参数化能力（以部分模板为例）：**

- **cat** — bodyWidth, headSize, earSize, tailLength, facing, eyeShape（round/slit/closed/happy）, mouthStyle（smile/neutral/open/frown）, whiskers
- **dog** — bodyWidth, headSize, earSize, tailLength, eyeShape, tongueOut, hasSpots, tailStyle（up/down/curly）
- **person** — headSize, bodyWidth, bodyHeight, expression（happy/sad/neutral/surprised/angry）, hairStyle（short/long/bald/ponytail）, accessories（glasses/hat/scarf/none）
- **tree** — canopySize, trunkWidth, leafDensity
- **house** — roofHeight, doorSize, bodyWidth

**语音指令示例：**
- "画一只开心的猫"
- "画一棵大树"
- "画一个戴眼镜的人"
- "画一只吐舌头的狗"

**设计亮点：**
- 所有模板支持中文别名映射（40+ 别名），语音识别结果可直接匹配
- 参数化设计使 LLM 能够根据用户语音中的形容词调整模板外观
- 部分模板包含渐变和阴影配置，增强视觉层次感

#### 2.1.3 基础图形模板（✅ 完整实现，15 种）

15 种几何/符号类模板：star, heart, arrow, chat_bubble, hexagon, octagon, diamond, cross, lightning, shield, cloud, crescent, music_note, infinity, hash

每个模板均有中文别名支持（如"五角星"→star, "红心"→heart, "对话框"→chat_bubble）。

#### 2.1.4 矢量图标搜索（✅ 完整实现，100+ 图标）

内置 100+ Lucide 矢量图标，支持语义搜索：

| 类别 | 数量 | 示例 |
|------|------|------|
| 箭头/方向 | 9 | arrow-right, chevron-up, move |
| 形状 | 10 | circle, square, triangle, pentagon |
| 人物 | 4 | user, users, user-plus, baby |
| 动物 | 6 | cat, dog, fish, bird |
| 自然 | 9 | sun, moon, cloud, flame, mountain |
| 食物 | 6 | apple, coffee, pizza, cake |
| 物品 | 14+ | home, car, key, camera, phone |
| UI 操作 | 22 | search, settings, edit, trash, download |
| 天气 | 6 | thermometer, wind, umbrella, zap |

**搜索算法：** 基于评分的匹配机制 — 图标名称（100/50 分）、关键词（80/30 分）、分类（20 分）。支持中英文查询。

**语音指令示例：**
- "画一个搜索图标"
- "放一个太阳在右上角"
- "画一个设置的图标"

#### 2.1.5 AI 图像生成（✅ 实现，使用 Pollinations.ai）

通过 Pollinations.ai 免费 API 生成图像，支持 8 种风格选项（realistic, cartoon, oil_painting, watercolor, pixel_art, sketch, cyberpunk, anime）。

**技术实现：**
- 所有图像统一追加涂鸦风格后缀，确保生成结果与手绘画布风格一致
- 使用 `multiply` 混合模式过滤白色背景，仅保留黑色线条
- 异步模式：先插入"加载中..."占位文本，完成后替换为实际图像
- 30 秒超时保护，失败时显示红色错误提示

**语音指令示例：**
- "画一个卡通风格的小狗"
- "生成一张水彩风格的风景画"

#### 2.1.6 SVG 路径生成（✅ 实整实现）

LLM 可直接生成 SVG path data，由前端渲染为图形。此能力使系统理论上可以绘制任意形状。

**语音指令示例：**
- "画一个不规则的闪电形状"
- "画一条波浪线"

#### 2.1.7 文本添加（✅ 基本实现）

支持在画布上添加文本节点，可指定字体大小、字体族、颜色和位置。

**语音指令示例：**
- "写一行字：你好世界"
- "在中间写上 VDC"

#### 2.1.8 节点操作（✅ 完整实现）

| 操作 | 工具 | 说明 |
|------|------|------|
| 修改 | modify_node | 支持位置、大小、颜色、旋转、透明度等属性的部分更新 |
| 删除 | delete_node | 支持单节点删除和级联删除 |
| 移动 | move_node | 绝对位置移动 |
| 对齐 | align_objects | 基于 kiwi.js 约束求解器的 4 种对齐关系 |
| 层级 | reorder_node | bring_to_top, send_to_back, bring_forward, send_backward |
| 查询 | query_canvas_state | 返回当前画布状态摘要 |

**语音指令示例：**
- "把猫移到右边"
- "把圆形放大一点"
- "把红色矩形放到最上面"
- "删除那个三角形"
- "让猫和狗顶部对齐"

#### 2.1.9 画布控制（✅ 完整实现）

| 操作 | 说明 |
|------|------|
| 背景颜色 | set_canvas_background，支持任意颜色值 |
| 撤销 | undo_action，支持单步和多步撤销 |
| 重做 | redo_action，支持单步和多步重做 |
| 清空 | clear_canvas，需 confirm=true 确认 |

**语音指令示例：**
- "把背景改成浅蓝色"
- "撤销上一步"
- "重做"
- "清空画布"

#### 2.1.10 语音合成回复（✅ 实现）

LLM 执行操作后可通过 TTS 语音回复用户，增强交互自然度。默认关闭，用户可手动开启。

---

### 2.2 计划但未实现的指令能力

#### 2.2.1 空间查询指令（❌ 未实现）

**计划能力：** 用户可以查询画布上某个对象的位置关系，如"猫在哪里"、"圆形在矩形的左边吗"。

**未实现原因：** 该功能需要 LLM 具备空间推理能力，当前 mimo 模型的工具调用机制更擅长"执行"而非"查询后推理"。`query_canvas_state` 工具可以返回节点列表，但缺乏结构化的空间关系查询接口。

**改进方向：** 可在 `query_canvas_state` 基础上增加 `spatial_query` 工具，返回指定节点的邻居节点、相对位置等结构化信息，供 LLM 进行空间推理。

#### 2.2.2 批量对齐与分布（❌ 未实现）

**计划能力：** 支持 distribute-h（水平均分）、distribute-v（垂直均分）以及 left/right/top/bottom 等 8 种对齐关系。

**未实现原因：** 当前约束求解器（kiwi.js）实现了 4 种基本对齐关系，已覆盖最常见的使用场景。批量分布需要多节点同时求解，实现复杂度较高，在有限开发周期内优先保证核心功能。

**改进方向：** 扩展 constraintSolver.ts，增加 distribute-h 和 distribute-v 约束类型，支持 3 个以上节点的均匀分布。

#### 2.2.3 批量删除指令（❌ 未实现）

**计划能力：** 支持"删除所有矩形"、"删除选中的对象"等批量删除指令。

**未实现原因：** 当前 delete_node 工具仅支持单节点删除。批量删除需要 LLM 先查询再逐个删除，多轮工具调用的可靠性随轮次增加而下降。

**改进方向：** 增加 `delete_nodes` 工具，支持按类型、按名称模式匹配的批量删除。

#### 2.2.4 边连线操作（❌ 未实现）

**计划能力：** 在两个节点之间创建连接线（edge），用于绘制流程图、关系图等。

**未实现原因：** CanvasStore 已支持 addEdge/deleteEdge，但未暴露为 LLM 可调用工具。连线的视觉渲染（箭头、弯曲度、标签）需要额外的渲染逻辑。

**改进方向：** 新增 `connect_nodes` 工具，在 roughRenderer 中实现连线渲染。

#### 2.2.5 分组操作（❌ 未实现）

**计划能力：** 将多个节点组合为一个整体，支持统一移动、缩放、删除。

**未实现原因：** 节点类型定义中已包含 group 字段，但分组的渲染逻辑（统一变换矩阵、嵌套渲染）和工具接口未实现。

**改进方向：** 新增 `group_nodes` 和 `ungroup_nodes` 工具，在渲染层实现分组变换。

#### 2.2.6 画布导出（❌ 未实现）

**计划能力：** 将画布导出为 PNG 或 SVG 文件。

**未实现原因：** 当前开发周期优先保证核心绘图功能。Konva 原生支持 `toDataURL()` 和 `toJSON()`，技术上可行但需要处理 rough.js 图层的导出兼容性。

**改进方向：** 利用 Konva 的导出 API，结合 rough.js 的 SVG 输出，实现 PNG/SVG 导出。

---

## 3. 语音理解准确性与容错性设计

### 3.1 已实现的容错机制

#### 3.1.1 语音识别纠错映射

在 `packages/shared/src/constants/index.ts` 中实现了三层精确匹配纠错：

| 纠错类别 | 条目数 | 说明 |
|----------|--------|------|
| 颜色名称纠错 | 20 条 | 处理中文同音字错误（如"红色"的常见误识别） |
| 形状名称纠错 | 18 条 | 处理形状名称的语音误识别 |
| CSS 属性纠错 | 12 条 | 处理属性名称的语音误识别 |

#### 3.1.2 LLM 系统提示词纠错

在 LLM 的 system prompt 中内嵌了完整的纠错映射表和工具选择指南，使 LLM 能够在语义层面纠正用户的模糊表达。例如：
- "画个圆的" → 理解为"画一个圆形"
- "把那个东西变大" → 结合上下文理解为放大最近创建的节点

#### 3.1.3 网格坐标系统

50×50 网格坐标系统（x1y1 到 x50y50）为 LLM 提供了直观的空间参考，避免了 LLM 直接计算像素坐标的困难。用户说"画在中间"，LLM 可以输出 `gridCoordinate: "x25y25"`，前端自动转换为实际像素位置。

#### 3.1.4 空间位置枚举

`SpatialPosition` 类型（center, top-left, top-right, bottom-left, bottom-right）提供了另一层抽象，进一步降低了 LLM 的空间推理负担。

#### 3.1.5 约束求解对齐

基于 kiwi.js 的约束求解器处理精确的空间关系计算。LLM 只需声明语义关系（如"rightOf"），求解器自动计算精确坐标，避免了 LLM 的数学计算误差。

#### 3.1.6 ASR 网络容错

- 网络错误自动重试 3 次，递增退避（1s, 2s, 3s）
- 识别中断自动重启
- `no-speech` 静默处理，不打扰用户

### 3.2 计划但未实现的容错机制

#### 3.2.1 领域热词注入（❌ 未实现）

**计划能力：** 将绘图领域常用词汇（如形状名称、颜色名称）注入 STT 解码器，提高专业术语的识别准确率。

**未实现原因：** Web Speech API 不支持自定义词表注入，需要使用云端 ASR 服务（如百度语音、讯飞）才能实现此能力。

**改进方向：** 迁移到支持自定义词表的 ASR 服务，或在 LLM 层面加强纠错。

#### 3.2.2 模糊匹配纠错（❌ 未实现）

**计划能力：** 使用 Levenshtein 距离进行模糊匹配，当精确匹配失败时尝试最相似的修正。

**未实现原因：** 当前精确匹配已覆盖最常见的误识别场景，模糊匹配可能引入误修正。在有限开发周期内优先保证核心功能。

**改进方向：** 在精确匹配失败时，计算输入与所有候选词的编辑距离，选择距离最小且低于阈值的候选词。

#### 3.2.3 多轮确认对话（❌ 未实现）

**计划能力：** 对破坏性操作（如清空画布、删除多个节点）进行语音确认，形成"你说要清空画布，确认吗？"的对话流程。

**未实现原因：** 当前 `clear_canvas` 工具通过 `confirm` 参数实现确认，但不是语音交互式的。多轮确认需要维护状态机，增加系统复杂度。

**改进方向：** 在 useVoiceAgent 中增加确认状态，当 LLM 调用破坏性工具时暂停执行，等待用户语音确认。

#### 3.2.4 上下文自动注入（❌ 未实现）

**计划能力：** 每次请求 LLM 前自动调用 `query_canvas_state` 获取当前画布状态，注入到 LLM 上下文中，使 LLM 能够理解"那个红色的圆"指代哪个节点。

**未实现原因：** 每次请求前额外调用一次 LLM（或工具）会增加延迟。当前实现依赖对话历史隐式传递上下文。

**改进方向：** 在发送请求前自动拼接当前画布状态摘要到用户消息中，不增加额外的 LLM 调用。

---

## 4. 响应延迟优化

### 4.1 已实现的优化

#### 4.1.1 脏检测渲染

KonvaRenderer 通过字符串快照进行脏检测，仅在状态实际变化时才触发重绘，避免了不必要的渲染开销。

#### 4.1.2 异步占位模式

AI 图像生成采用"先占位、后替换"模式，用户说完后立即看到"加载中"文本，避免 UI 卡顿感。

#### 4.1.3 多轮工具调用上限

LLM 的工具调用轮次上限设为 5 轮，防止无限循环调用导致的延迟累积。

#### 4.1.4 处理锁机制

`isProcessingRef` 防止并发 LLM 请求，确保每次只有一个请求在处理中，避免资源竞争。

#### 4.1.5 网格坐标系统

50×50 网格坐标系统减少了 LLM 需要进行的数学计算量，降低了工具调用的推理延迟。

### 4.2 延迟瓶颈与改进方向

| 瓶颈 | 当前情况 | 改进方向 |
|------|----------|----------|
| ASR 延迟 | 依赖 Web Speech API，延迟不可控 | 迁移到流式 ASR 服务，支持边说边识别 |
| LLM 推理延迟 | mimo-v2.5-pro 云端推理，延迟取决于网络 | 考虑本地小模型作为快速响应层 |
| 多轮工具调用 | 复杂指令可能触发 3-5 轮调用 | 优化 system prompt，提高单轮调用准确率 |
| 图像生成延迟 | Pollinations.ai 30 秒超时 | 实现图像缓存，相同 prompt 复用结果 |

---

## 5. 复杂指令拆解与执行能力

### 5.1 已实现的拆解能力

#### 5.1.1 LLM 多轮工具调用

系统支持 LLM 在单次用户输入后进行最多 5 轮工具调用。例如用户说"画一只猫和一棵树"，LLM 可以：
1. 第 1 轮：调用 `generate_template` 创建猫
2. 第 2 轮：调用 `generate_template` 创建树

#### 5.1.2 System Prompt 场景组合指南

LLM 的系统提示词中包含了完整的场景组合工作流指南：
1. 创建各个元素
2. 调整位置
3. 对齐关系
4. 必要时调整层级

**语音指令示例：**
- "画一只猫坐在屋顶上" → 生成 house → 生成 cat → 移动 cat 到屋顶位置
- "画一个太阳在天空，下面画一座山和一棵树" → 依次生成三个元素并定位

#### 5.1.3 修改指令理解

LLM 能够理解修改类指令并映射到正确的工具调用：
- "把猫放大一点" → modify_node + scaleX/scaleY 1.3
- "把圆形变成红色" → modify_node + fill: red
- "把矩形旋转 45 度" → modify_node + rotation: 45

### 5.2 拆解能力的局限与改进方向

#### 5.2.1 复合意图完全依赖 LLM

当前的指令拆解完全依赖 LLM 的推理能力，没有前端层面的指令解析器。当 LLM 推理失败时，系统无法回退到规则匹配。

**改进方向：** 实现一个轻量级的前端指令解析器，作为 LLM 不可用时的降级方案。至少支持"画一个 + 颜色 + 形状"的基本模式。

#### 5.2.2 代词指代依赖 LLM

"把它移到右边"中的"它"完全依赖 LLM 通过对话历史进行指代消解。当对话历史较长时，LLM 可能无法正确识别指代对象。

**改进方向：** 实现上下文自动注入，在每次请求前将"最近操作的节点"信息显式传递给 LLM。

#### 5.2.3 缺少中断/队列策略

当前仅通过处理锁防止并发请求，没有中断当前操作、排队等待或丢弃新指令的策略。

**改进方向：** 实现指令队列，支持"中断当前操作"语音指令，并在新指令到达时提供排队或覆盖的选项。

---

## 6. 技术架构总结

### 6.1 管线架构

```
用户语音输入
    ↓
Web Speech API (ASR)
    ↓  ← 语音纠错映射（颜色/形状/属性）
文本指令
    ↓
mimo-v2.5-pro LLM (OpenAI 兼容 API)
    ↓  ← System Prompt（网格系统 + 工具指南 + 模板目录）
    ↓  ← 多轮 Tool Calling（最多 5 轮）
16 种画布工具调用
    ↓
CanvasStore（状态更新）
    ↓  ← 订阅/通知机制
KonvaRenderer + rough.js
    ↓
画布展示手绘风格图形
    ↓  ← 可选 TTS 语音回复
```

### 6.2 核心模块

| 模块 | 职责 | 关键设计 |
|------|------|----------|
| @vdc/shared | 类型、常量、工具函数 | 语音纠错映射、网格坐标转换 |
| @vdc/canvas-engine | 状态管理 + 渲染 | 快照式撤销/重做、脏检测、约束求解 |
| @vdc/voice-agent | 工具定义 + 服务 | 42 种参数化模板、100+ 图标搜索 |
| @vdc/web | 前端应用 | 语音管线 Hook、玻璃拟态 UI |

### 6.3 数据持久化

- **当前方案：** localStorage 自动保存，每次状态变更自动写入
- **容量限制：** 约 5MB，适合单用户单画布场景
- **导入/导出：** 支持 `.canvas` JSON 文件的导入导出

---

## 7. 总结

### 7.1 已达成的能力

- ✅ 纯语音控制绘图，全程无需鼠标/键盘
- ✅ 16 种 LLM 可调用画布工具，覆盖图形生成、编辑、删除、对齐、撤销重做等完整操作
- ✅ 42 种参数化复杂模板 + 15 种基础模板 + 100+ 矢量图标
- ✅ AI 图像生成（Pollinations.ai）
- ✅ 语音识别纠错映射（50 条精确匹配规则）
- ✅ 网格坐标系统 + 空间位置枚举，降低 LLM 空间推理难度
- ✅ kiwi.js 约束求解器，实现精确对齐
- ✅ ASR 网络容错（自动重试、自动重启）
- ✅ 多轮工具调用实现复杂指令拆解
- ✅ 手绘风格渲染（rough.js）
- ✅ 50 步撤销/重做 + localStorage 自动持久化

### 7.2 改进目标

| 优先级 | 改进项 | 预期收益 |
|--------|--------|----------|
| 高 | 上下文自动注入（画布状态摘要） | 提升 LLM 对指代和空间关系的理解 |
| 高 | reorder_node 工具暴露给 LLM | 补全层级控制能力 |
| 高 | 批量删除工具 | 支持"删除所有矩形"等指令 |
| 中 | 模糊匹配纠错（Levenshtein） | 提升语音纠错覆盖率 |
| 中 | 多轮语音确认对话 | 提升破坏性操作的安全性 |
| 中 | 画布导出（PNG/SVG） | 支持作品分享 |
| 低 | 领域热词注入 ASR | 提升专业术语识别率 |
| 低 | 分布对齐（distribute-h/v） | 支持多节点均匀分布 |
| 低 | 边连线工具 | 支持流程图/关系图绘制 |
| 低 | 本地 LLM 降级方案 | 离线可用性保障 |
