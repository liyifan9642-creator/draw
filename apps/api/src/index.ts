/**
 * @vdc/api — 后端 API 服务
 *
 * 提供：
 * - 画布保存/加载 (POST /api/canvas/:id/save, GET /api/canvas/:id)
 * - 图像生成代理 (POST /api/image/generate)
 * - 版本历史 (GET /api/canvas/:id/history)
 * - 画布导出 (POST /api/canvas/:id/export)
 *
 * TODO: Phase 4 实现
 */
